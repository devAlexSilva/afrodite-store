import blockContent from './schemas/blockContent'
import category from './schemas/category'
import product from './schemas/product'

export const schema = {
  types: [category, blockContent, product],
}
